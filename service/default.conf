server {
  listen 80;
  server_name localhost;
  location /api {
    proxy_pass http://backend:80;
  }
  location /oauth {
    proxy_pass http://backend:80;
  }
  location / {
    root /old_frontend/;
    error_page  404 =200 /index.html;
  }
  # Will use this section when old ons is depracated.
  #
  # location / {
  #   proxy_pass http://frontend:3000;
  # }
}